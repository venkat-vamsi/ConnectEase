#spring.application.name=connectease
#
## Database Connection
#spring.datasource.url=jdbc:mysql://localhost:3306/connecteasedb
#spring.datasource.username=root
#spring.datasource.password=root
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## JPA & Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#
## Open-in-view warning disable (Best practice)
#spring.jpa.open-in-view=false
#server.port=8081
#
#gemini.api.key=GEMINI_API_KEY

spring.application.name=connectease

# Database Connection (Railway injects these automatically)
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Open-in-view warning disable
spring.jpa.open-in-view=false

# Port (Railway assigns a random port, so this is mandatory)
server.port=${PORT:8081}

# Gemini API Key
gemini.api.key=${GEMINI_API_KEY}